version: 0.0
os: linux
files:
  - source: ./AMAZING-/dist
    destination: /usr/share/nginx
    overwrite: yes

permissions:
  - object: /usr/share/nginx
    owner: root
    group: root
    mode: 755

hooks:
  AfterInstall:
    - location: scripts/after_deploy.sh
      timeout: 300
      runas: root
